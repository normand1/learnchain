# LearnChain v0.4.2

This patch release tightens a cross-platform test issue and makes the main menu more explicit about both the preferred deep-dive workflow and the running app version.

## Changed

- Moved `Generate session deep dive` to the top of the main action list so the document workflow is the first primary menu action.
- Added the current app version to the main menu header.

## Fixed

- Fixed Windows test failures in Claude deep-dive file reference extraction fixtures by generating valid JSON tool arguments across platforms.
